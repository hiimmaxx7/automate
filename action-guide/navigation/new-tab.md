# 新标签

新标签是一个控制浏览器自动打开一个新的空标签（tab）在正在运行的Profile中，类似于您在浏览器的标签栏上点击`+`号的操作。

🎥 查看更多教程视频：[在这里](https://youtu.be/a9qNt96X1SM)。

#### 实际示例：打开新标签以并行执行任务

当您在当前标签上进行数据抓取时，但需要打开另一个网站（例如：检查邮件的页面或获取加密货币信息的页面）以获取协调信息，而不想中断旧页面：

* 配置方法：将New tab操作拖入流程。