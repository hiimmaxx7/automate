---
description: 顺序指令块 (Normal block)
---

# Normal block

这是用于容纳操作的最基本的指令块。您只需将各个操作放入其中,系统就会自动按从上到下的顺序依次执行,完成这个操作后才会转到下一个操作。

* 作用:用于将具有相同目的的操作分组整理,让脚本看起来更简洁,也更容易管理。

💡 提示:当脚本过长时,您应该将其拆分成多个独立的模块(例如:登录模块、互动模块、修改密码模块等),这样以后需要针对某个部分进行调试或修改时会非常快捷,也不会造成混乱。