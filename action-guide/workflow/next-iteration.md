# 下一次循环

下一次循环是一个动作，用于立即停止当前循环中的后续动作，跳过循环块（For 或 While）中剩余的代码，直接进入下一次循环。

与 Exit loop（完全退出，停止循环）不同，下一次循环仅跳过当前正在运行的循环，并继续执行后续的循环，直到列表结束。这个动作通常放置在 If 语句块中，以过滤掉错误或不合适的数据。

<figure><img src="../../.gitbook/assets/image (139).png" alt=""><figcaption></figcaption></figure>