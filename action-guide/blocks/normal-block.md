---
description: 正常块
---

# Normal block

这是最基本的指令块，用于包含各种动作。您可以将动作放入其中，系统将自动从上到下依次运行，完成这一项后再转到下一项。

* 作用：用于将具有相同目的的动作分组，以便更紧凑，脚本将更清晰且更易于管理。

💡 提示：当脚本过长时，您应该将其拆分为单独的块（例如：登录块、交互块、修改密码块...），这样在需要修复错误或修改某个部分时也会非常快速且不会混乱。