---
description: 删除应用程序的所有数据，将应用恢复到初始安装状态。
---

# 清理应用数据

清理应用数据是删除应用程序的所有数据和缓存，将应用恢复到首次安装时的干净状态（注销账户，丢失所有设置）。在需要使用新账户重新运行应用时非常有用。

#### 配置参数说明：

* 包名：需要删除数据的应用程序的包名，例如 `com.facebook.katana`。

<figure><img src="../../.gitbook/assets/pa-06.png" alt=""><figcaption></figcaption></figure>