# 关闭标签

关闭标签是指令浏览器立即关闭当前激活的标签页（tab）的操作。此操作没有任何附带的配置参数。

🎥 查看更多教程视频：[在这里](https://youtu.be/a9qNt96X1SM)。

#### 实际示例：完成辅助任务后清理屏幕

当您打开一个辅助标签以从电子邮件中获取验证码或获取电子钱包信息时，在成功将所需数据复制到变量后，您需要关闭该标签以释放RAM并保持浏览器界面的整洁：

* 处理流程：
  1. 在辅助标签中成功获取OTP码。
  2. 将关闭标签操作拖入脚本中。
  3. 调用活动标签操作（索引 = `0`）以将控制权返回到主工作标签。