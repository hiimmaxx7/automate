# Next loop

Next loop 是一种用于立即停止当前循环中其后续动作的操作,跳过循环块(For 或 While)中剩余的代码,直接进入下一次循环。

与 Exit loop(彻底退出,直接终止循环)不同,Next loop 只会跳过当前正在执行的这一次循环,并继续执行后续的循环,直到列表结束为止。此操作通常放置在 If 语句块内,用于过滤掉错误或不符合条件的数据。

<figure><img src="../../.gitbook/assets/image (139).png" alt=""><figcaption></figcaption></figure>